# CLAUDE.md — Can You Beat Wellington?

Project tracking, backlog, and architectural decisions. Completed backlog items and the full Next.js + Neon migration history live in [`CLAUDE_ARCHIVE.md`](CLAUDE_ARCHIVE.md) — this file stays focused on what's current and what's still open.

## Project Overview

A hobby webapp that checks if today's weather in Wellington, NZ is good enough that you "can't beat it". Users can vote agree/disagree. Historical data stored in Neon (Postgres), visualised on the About page. Fully migrated from a Vite SPA + Supabase to Next.js + Neon on 2026-06-28 — see the Changelog below, and `CLAUDE_ARCHIVE.md` for the full rebuild history.

- **Live**: https://canyoubeatwellington.nz/ (redirects from the old `canyoubeatwellington.radomski.co.nz`, which stays live only for the *old* ActivityPub actor identity now — see Infrastructure below)
- **Repo**: https://github.com/patatrat/CanYouBeatWellington
- **Stack**: Next.js 16 (App Router) + TypeScript + Tailwind CSS + shadcn/ui + Neon (`@neondatabase/serverless`) + Vercel KV (ActivityPub) + Vitest
- **Analytics**: Vercel Analytics
- **Hosting**: Vercel (auto-deploys on push to `main`; `staging` branch deploys to a Vercel preview for pre-production testing)
- **Originally built with**: Lovable (formerly GPT Engineer) — legacy files removed

## Weather Assessment Rules

A "good day" requires ALL of (thresholds vary by season — see `rulesStorage.ts`):
- Max temperature ≥ seasonal minimum (13°C winter → 19°C summer)
- Max wind speed < 30 km/h (year-round)
- Daytime rain ≤ 0.1mm (year-round) — a trace tolerance, not a literal zero (see Changelog, 2026-08-10)

Six seasons: Summer (Jan–Mar), Autumn (Apr–Jun), Winter (Jul–Aug), Spring 1 (Sep), Shitsville (Oct–Nov), Spring 2 (Dec).
Computed at runtime from `rulesStorage.ts` — not stored in DB, so rules can change freely.
Data source: Open-Meteo API (free, no key required).

The verdict itself is based on actual temperature, not how it feels — but the coldest daytime **apparent ("feels like") temperature** is also fetched and stored (`daily_weather_records.feels_like`, wind chill/humidity/radiation-adjusted via Open-Meteo's `apparent_temperature`), and **snowfall** (cm, `daily_weather_records.snowfall`, from Open-Meteo's `snowfall_sum`) the same way. Both feed the `FEELS_LIKE_COLD`/`SNOW` severity quip tiers (see Key files → `quips.ts`) rather than the verdict itself, and aren't otherwise surfaced in the UI.

A non-weather **verdict override** can also force the verdict either way — see Special Dates below.

## Special Dates

The home page, daily cron, and History calendar all check a `special_date_defs`/`special_date_occurrences` Neon schema for active occasions on a given day — data layer in `src/lib/special-dates.ts`. Each occasion can override the quip, swap the background, trigger a confetti/balloon effect, link to an event page, and (sporting events only) flip the entire weather verdict ("a Wellington team winning forces a good day regardless of weather"). Three kinds:

A special date's quip can vary by weather scenario rather than being fixed — `scenario_quips` (JSONB, partial map of `good`/`great`/`cold`/`rain`/`wind`/`all_bad` → quip text) is checked via `getSpecialDayScenario()`/`getSpecialDayQuip()` in `special-dates-logic.ts` whenever that def is the day's active special date, ahead of the global scenario quips but behind both `quip_override` (a single fixed string — still wins outright when set, e.g. for sporting results where the weather is beside the point) and the global severity quips (extreme wind/rain still takes priority over holiday flavour text). A scenario with no entry falls through to the global quip system as normal.

- **`fixed_rule`** — annual holidays computed from a small rule DSL (`fixed:month:day`, `nth_weekday:n:month:weekday`, `easter_offset:days`), auto-regenerated each year by the daily cron (`ensureUpcomingOccurrences()`) — self-healing, no separate yearly cron needed
- **`moveable`** — festivals with no formula (gazetted/announced yearly); a new occurrence row needs entering by hand each year
- **`sporting`** — one-off fixtures; created `pending` ahead of a game, resolved afterward via `POST /api/admin/resolve-sporting` (`ADMIN_SECRET` bearer auth, same pattern as `CRON_SECRET`)

Current catalogue (`special_date_defs` — occurrence dates live in Neon, not here, since they change yearly):

| Slug | Kind | Rule / notes | Effect | Background |
|------|------|---------------|--------|------------|
| `new-years-day` | fixed_rule | Jan 1, has `scenario_quips.good` | confetti | festive |
| `new-years-eve` | fixed_rule | Dec 31, has `scenario_quips.good` | confetti | festive |
| `day-after-new-years` | fixed_rule | Jan 2 | none | — |
| `wellington-anniversary-day` | fixed_rule | 4th Mon Jan | confetti | anniversary |
| `waitangi-day` | fixed_rule | Feb 6 | none | — |
| `good-friday` | fixed_rule | Easter Sunday − 2 days | none | — |
| `easter-monday` | fixed_rule | Easter Sunday + 1 day | none | — |
| `anzac-day` | fixed_rule | Apr 25 | none | — |
| `kings-birthday` | fixed_rule | 1st Mon Jun | none | — |
| `labour-day` | fixed_rule | 4th Mon Oct | none | — |
| `christmas-day` | fixed_rule | Dec 25, has `scenario_quips` for good/rain/cold/all_bad | confetti | festive |
| `boxing-day` | fixed_rule | Dec 26 | none | — |
| `matariki` | moveable | gazetted yearly, no formula (2026: Jul 10, 2027: Jun 25) | none | matariki |
| `cuba-dupa` | moveable | usually late March | confetti | — |
| `wow` | moveable | World of WearableArt, usually Sep–Oct | confetti | — |
| `beervana` | moveable | usually August | confetti | — |
| `hurricanes-grand-final-2026` | sporting | won 2026-06-20 vs Chiefs — `verdict_override=true`, `confirmed` | confetti | rugby |
| `all-blacks-v-italy-2026-07-11` | sporting | 2026-07-11, Sky Stadium — `pending`, awaiting resolution | confetti | rugby |

Solemn/civic holidays (ANZAC Day, Waitangi Day, Good Friday, King's Birthday, Labour Day) deliberately have no celebration effect. When two occasions overlap a day, `pickPrimarySpecialDate()` picks the narrower date range, tie-broken by start date then occurrence id — see `src/lib/__tests__/special-dates.test.ts`.

## Infrastructure

| Concern | Solution |
|---------|---------|
| Hosting | Vercel (auto-deploys on push to `main`/`staging`) |
| Database | Neon Postgres, free tier (project: `can-you-beat-wellington` / `dawn-queen-51598624`) — no auto-pause, unlike Supabase |
| Daily weather + fediverse fan-out | Vercel Cron at 21:30 UTC daily (`/api/cron/daily-weather`) — ≈10am NZT (10:30 NZDT / 9:30 NZST; Vercel Cron is UTC-only, no timezone option, so this drifts ±30 min across NZ's DST transition) |
| ActivityPub federation | Vercel KV (Upstash Redis) — two actors now coexist, each with its own follower set/post list (`cybw:ap:followers`/`cybw:posts` for the old actor, `cybw:ap:followers:new`/`cybw:posts:new` for the new one) — see `src/lib/ap-identity.ts` |
| CI | GitHub Actions (`ci.yml`) — lint + type check + test + build + audit on every push/PR to `main`/`staging` |
| Secrets | GitHub repo secrets + Vercel env vars — `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `CRON_SECRET`, `ADMIN_SECRET`, `AP_PUBLIC_KEY`/`AP_PRIVATE_KEY` (old actor), `AP_PUBLIC_KEY_NEW`/`AP_PRIVATE_KEY_NEW` (new actor), `KV_REST_API_URL`, `KV_REST_API_TOKEN` |
| DNS | Two domains, two registrars/DNS setups: `canyoubeatwellington.radomski.co.nz` via Cloudflare (DNS-only, grey cloud) → Vercel (kept live solely for the *old* ActivityPub actor identity — see below); `canyoubeatwellington.nz` via its own AWS Route 53 hosted zone → Vercel, the human-facing canonical domain **and** the new actor's identity. The bare apex is canonical (not `www`) — Vercel's own domain redirect was flipped to `www → apex` (was the reverse originally); `middleware.ts` 308-redirects the old domain straight to the apex for every path except `/actor`, `/.well-known/webfinger`, `/notes`, `/quote-authorizations`. IRPO (NZ's WHOIS privacy option) not yet enabled on the new domain — pending. |
| **Legacy (pending removal)** | Supabase project still exists as an archived snapshot; `VITE_SUPABASE_*`/`SUPABASE_*` env vars already removed from Vercel/GitHub |

---

## Backlog

### Quip backlog — batched, not yet released
Collecting quip ideas here as they come up instead of shipping each one as its own small release (previous sessions did several one-off quip PRs in a row — batching avoids the release-noise). Implement and ship together in one pass when there's a decent batch, then clear this section.

Empty right now — the previous batch (general ALL_BAD quip, SNOW + FEELS_LIKE_COLD severity tiers, Christmas Day/New Year's Eve/New Year's Day scenario quips) shipped 2026-08-03, prompted by an actual snow shower and wanting the SNOW tier live before the next one. See Changelog for what went out.

### Other scenario ideas floated, not yet drafted
Brainstormed while adding the feels-like/snow tiers, no quip text written yet — free to draft any of these whenever, same batching process:
- **Fog** — Wellington Airport's fog delays/cancellations are locally famous; `weather_code` is already fetched (used today only for `calculateSunniness`), WMO codes 45/48 are fog, so this is a zero-new-data-cost severity tier.
- **Dead calm / no wind** — the inverse of the wind severity tiers; Wellington's reputation as "the windy city" makes a genuinely still day (e.g. wind < 5 km/h) arguably as newsworthy as snow, just in the other direction. Also zero new cost — `wind_speed` already fetched.
- **Extreme heat** — pairs with FEELS_LIKE_COLD; Wellington rarely breaks 25°C, so a genuinely hot day (distinct from GREAT_DAY's gentler +3°-above-threshold bar) could get its own callout. Zero new cost — `temperature` already fetched.
- Lower priority: hail (WMO 96/99, possible from existing `weather_code` data but rarer/less distinctly-Wellington than fog); UV/extreme sun (would need a genuinely new Open-Meteo field, more setup cost).

### P2 — Post-migration cleanup
- [x] **Run the Neon schema migration for `snowfall`** (2026-08-03) — `daily_weather_records.snowfall` added by hand in the Neon SQL editor (automated DDL was permission-blocked, same pattern as the migrations below); confirmed live via `information_schema.columns` before pushing. Deployed same day.
- [x] **Run the Neon schema migration for `feels_like` + `scenario_quips`** (2026-08-03) — `daily_weather_records.feels_like` and `special_date_defs.scenario_quips` added by hand in the Neon SQL editor (automated DDL was permission-blocked); confirmed live via `information_schema.columns` before pushing. Deployed same day.
- [x] **Run the occurrence unique index in Neon** (2026-07-02) — `special_date_occurrences_def_start_key ON (def_id, start_date)` created in production by hand (automated DDL was permission-blocked); `db/schema.sql` documents it, and `ensureUpcomingOccurrences()`'s `ON CONFLICT DO NOTHING` race guard is now fully backed.
- [ ] **Delete the Supabase project** (Settings → General → Delete project) once production has been stable for a while — deliberately holding off; all data already migrated and verified.
- [x] **Resolve the `next`-bundled `postcss` moderate vulnerability** (2026-07-14) — no stable Next carries the fix (16.2.10 still pins postcss 8.4.31; only 16.3 canaries have 8.5.10), so an npm `overrides` entry in `package.json` forces `next`'s nested copy to 8.5.10 (semver-minor, API-compatible). `npm audit` reports 0 vulnerabilities; Dependabot alert #65 closes with it.
- [x] **Bump the `postcss` override again, 8.5.10 → 8.5.19** (2026-07-24) — a second, unrelated `postcss` advisory (arbitrary file read via attacker-controlled `sourceMappingURL` in CSS comments, high severity, patched at 8.5.12) was published after the first fix landed; the exact `8.5.10` pin from 2026-07-14 fell squarely in the new vulnerable range. This time CI's `Audit` step caught it on `staging` *before* the fast-forward to `main` — exactly the failure mode the item below is about, working as intended.
- [ ] **Drop the `postcss` override once Next 16.3.x stable ships** — the `overrides.next.postcss` entry in `package.json` becomes redundant when Next bundles a patched version itself; remove it during that upgrade so npm resolves Next's own pin again.
- [x] **Resolve four more Dependabot alerts surfaced 2026-07-23** — `next`/`eslint-config-next` bumped 16.2.9 → 16.2.11 (fixes nine Next.js CVEs: SSRF in Server Actions/rewrites, DoS, cache confusion, unauthenticated Server Function disclosure — a real runtime fix, unlike the build-time-only postcss issue); `sharp` overridden 0.34.5 → 0.35.3 (libvips CVEs; safe since the app never imports `next/image`, so the vulnerable code path is dead weight regardless); `brace-expansion` overridden per-exact-version (`minimatch@3.1.5` → 1.1.16, `minimatch@10.2.5` → 5.0.7 — two different consumers pin different brace-expansion majors, so the override keys target the exact `minimatch` version via npm's `"pkg@version"` override syntax rather than a blanket bump that would break the older one); `js-yaml` overridden 4.2.0 → 4.3.0 under `eslint`'s own `@eslint/eslintrc` dependency. All four were dev-tooling/build-time except the Next.js bump. `npm audit` back to 0 vulnerabilities.
- [ ] **Fix CI's silent audit gap** — the two commits that introduced these four vulnerabilities (the severity-quips feature commit, unrelated to the deps themselves) failed CI's `Audit` step on both `staging` and `main` after push, and were merged/fast-forwarded before that was noticed. `ci.yml`'s `Audit` step already runs `--audit-level=high` and should have blocked the merge — worth adding a branch-protection check requiring CI to pass before `main` accepts a fast-forward, since right now a red Audit run doesn't stop anything.
- [x] **Scope CI's `Audit` step to production dependencies** (2026-07-24) — `npm audit --audit-level=high` → `--omit=dev`. Root cause: `GHSA-mh99-v99m-4gvg` (`brace-expansion` unbounded-length DoS) has no fix compatible with the old `minimatch@2.x/3.x` line that ESLint's own plugin ecosystem (`eslint-plugin-import`/`-jsx-a11y`/`-react`, `@eslint/config-array`, `@eslint/eslintrc`) still depends on — the only listed patch (5.0.8) changes the module's export shape (`module.exports = fn` → `{ expand: fn }`), confirmed by direct test to break `require('brace-expansion')(str)` call sites. Tried bumping `eslint` 9→10 first (its *own* dependency moves to modern `minimatch@^10`), but `eslint-config-next`'s bundled plugins pull the vulnerable chain in independently of the top-level `eslint` version, so it didn't help and was reverted. That chain is 100% devDependency/build-time lint tooling — never shipped, never touches untrusted input — so gating CI on it forces either a broken lint setup or a permanently-red pipeline for a finding with no real exposure. `--omit=dev` reports 0 vulnerabilities; re-add full-scope auditing once upstream ships a compatible `minimatch` bump (worth an occasional `npm audit` check, not just waiting for Dependabot — dev-scope alerts don't always surface the same way).
- [x] **Resolve two more Dependabot alerts surfaced 2026-07-24** — a *third*, distinct `postcss` advisory (path traversal via `sourceMappingURL`, patched at 8.5.18) was published after the 8.5.19 bump above landed — that bump already cleared it, but it exposed that the override was scoped to `next`'s nested copy only, missing two *other* independent `postcss` installs (`@tailwindcss/postcss`, `vitest`→`vite`) that were still on 8.5.16. Moved the override to top-level (`overrides.postcss`, not nested under `next`) so all three resolve to the same patched version. The `brace-expansion` DoS above is the second alert, tracked as "no fix available" rather than resolved.
- [x] **Applied Dependabot's grouped update PRs #48/#49 by hand, minus `typescript`** (2026-08-05) — both PRs bundled a `typescript` 6→7 bump that breaks CI outright (`typescript-eslint` doesn't support TS 7.0 yet — tracked at typescript-eslint#10940, not fixable here). Applied every other package manually (`next`/`eslint-config-next`/`react`/`react-dom` needed direct `package.json` edits since they're exact-pinned, same as before; the rest via `npm update` within existing caret ranges) and closed/commented on the PRs explaining why. A *fourth* postcss advisory (incomplete fix of the third one, patched at 8.5.23) turned up while reinstalling — bumped the override to 8.5.25. Dependabot has since re-opened the `typescript` bump alone as PR #50 (commented with the same explanation) — leave it open until typescript-eslint ships TS 7 support; `npm audit --omit=dev` and `gh api .../dependabot/alerts` both confirm 0 outstanding vulnerabilities in the meantime.

### P4 — Feature improvements
- [x] **Allow Mastodon quote posts on outgoing Notes** (2026-08-03, completed 2026-08-07) — added `interactionPolicy.canQuote.automaticApproval` + the matching `@context` terms to every outgoing Note in `ap-posting.ts` and `scripts/announce.js`. This static declaration alone fixed the outright "you are not allowed to quote this" block, but wasn't the full mechanism — see the `QuoteRequest`/`QuoteAuthorization` handshake entry directly below, added 2026-08-07, which is what actually gets a quote out of "pending" forever. Also fixed a shape bug found while implementing that: `automaticApproval` was an array (`[Public]`) instead of the bare string the FEP-044f spec and Mastodon actually expect — a "looks like it works but doesn't" bug, since the Quote button appearing was never blocked by this, only the pending-forever symptom was.
- [x] **Implement the FEP-044f `QuoteRequest`/`QuoteAuthorization` inbox handshake** (2026-08-07) — the missing half of the item above. `actor/inbox/route.ts` now handles incoming `QuoteRequest` activities: verifies the claimed actor matches the HTTP-signature signer (same pattern as Follow/Undo), resolves the quoted post against our own `cybw:post:<suffix>` KV store and ignores anything that isn't a real match (no authorization minted for posts that don't exist), handles `instrument` as either a bare URL or a fully embedded object (the spec's own examples show both), mints a `QuoteAuthorization`, stores it with no TTL (unlike notes' 1yr) since other instances can dereference it at any future point to re-verify, and sends a signed `Accept` back. New `GET /quote-authorizations/[id]` serves that object; added to `middleware.ts`'s AP-pinned path list alongside `/actor`/`/notes`/webfinger so it isn't swept into the `canyoubeatwellington.nz` redirect. Verified end-to-end in production, not just locally: fetched a live post's JSON to confirm the corrected `automaticApproval` shape, then `@Pat@mastodon.nz` quoted a real post — Vercel runtime logs show `Inbox: approved QuoteRequest` and a subsequent `GET /quote-authorizations/<id> 200` from mastodon.nz verifying it, and the quote displayed without a pending state. Along the way, discovered the 2026-08-03 backfill below hadn't actually stuck (the "already backfilled" post had no `interactionPolicy` at all when checked live on 2026-08-07, for unknown reasons) — re-ran `refresh-note` against it, which is also what produced the post used for this test.
- [x] **Backfill `interactionPolicy` onto the post already published before the quote-posts fix** (2026-08-03) — the fix above only changes what *future* posts contain; the daily post that had already gone out earlier the same day (10:22am NZT) was built and delivered before it, so its stored KV object (what `/notes/[id]` serves on a fresh fetch) and every follower's already-cached copy (from the original inbox push) both predate it. Added `POST /api/admin/refresh-note` (`ADMIN_SECRET` bearer auth, same pattern as the other admin routes) — rewrites the stored object with the current `interactionPolicy`/`@context` shape and sends a signed `Update{Note}` to current followers so their cached copies re-fetch. Extracted the parallel-delivery-with-timeouts logic out of `postToFollowers()` into a shared `deliverToFollowers()` in `ap-posting.ts` so this didn't need a third copy of it (the first two are `postToFollowers` and `scripts/announce.js`, which still has its own since it's outside the Next app). Scoped to one post on request, not a bulk backfill of history — `noteIdSuffix` is a parameter, so it can be re-run per-post if more are needed.
- [x] **Migrate the ActivityPub actor to canyoubeatwellington.nz via a real FEP-compliant Move, not just a redirect** (2026-08-09) — a redirect alone can't migrate followers; Mastodon needs `alsoKnownAs`/`movedTo` cross-references plus a signed `Move` activity. New `src/lib/ap-identity.ts` replaces 8 independently-hardcoded domain declarations with one shared module defining both actor identities (`OLD_ACTOR`/`NEW_ACTOR`) and two resolvers — `actorForHost()` (Host header, for `/actor`/`inbox`/`outbox`/`followers`) and `actorForWebfingerDomain()` (the `resource` param's domain part, since WebFinger identifies a domain textually, not via which host answered the request). This also fixed a live bug: hitting `/actor` on the new domain previously returned an object self-identifying as the old domain. New actor's endpoints live at the bare apex `canyoubeatwellington.nz` (matches the DNS canonical-domain switch above — originally built pointing at `www` before that switch happened, then simplified once the apex became canonical), with its own RSA keypair (`AP_PUBLIC_KEY_NEW`/`AP_PRIVATE_KEY_NEW`, `scripts/gen-ap-keys.js` run a second time) and `alsoKnownAs` pointing at the old actor; the old actor's document now carries `movedTo` pointing at the new one — both fields live and independently verifiable before the Move itself was ever sent, since publishing them is just a declarative document change, not a fediverse-wide broadcast. `src/lib/ap-move.ts` + `POST /api/admin/send-move` (`ADMIN_SECRET` bearer auth) build and can deliver the signed `Move`, reusing the same `deliverToFollowers()` fan-out Create/Update already use (now takes the actor explicitly as a parameter, since it's shared by callers needing different identities — the old actor for `refresh-note`/`send-move`, the new actor for daily posting).
- [x] **Fix WebFinger username matching being case-sensitive** (2026-08-09) — found while verifying the new actor: `@canyoubeat@canyoubeatwellington.nz` (lowercase, i.e. what a person actually types searching for the account) 404'd on *both* actors, a pre-existing bug that predates this session's work, never noticed before since the 3 original followers presumably found/followed via a clickable link rather than typing the handle by hand. The regex required exact `CanYouBeat` casing; acct: URIs aren't guaranteed to arrive in any particular case. Fixed with a case-insensitive match, extracted as `resolveWebfingerResource()` for unit testing.
- [x] **Send the Move and switch daily posting to the new actor** (2026-08-09) — sent from the old actor's existing key (Mastodon's Move semantics: `actor`/`object` are the old actor's id, `target` the new one), confirmed via direct Vercel log evidence rather than just follower-count inference: `tazyas`'s and `archaelus`'s servers each sent a fresh `Follow` to the new actor immediately followed by an `Undo{Follow}` to the old one — one of the two (`tazyas`) actually migrated *before* the Move was technically sent, most likely because their instance did a routine profile refresh, noticed the already-live `movedTo` field, and self-migrated proactively, independent of receiving the Move activity itself. `postToFollowers()` (the daily cron's fan-out) then switched from the old actor to the new one — left deliberately unswitched until the Move actually completed, since flipping it earlier would have meant new followers on the new actor got no daily posts while migration was still in progress. `deliverToFollowers()`/`actor/outbox`/`actor/inbox`'s `QuoteRequest` handling all generalized to work correctly for either actor as part of this (outbox was hardcoded to always show empty for the new actor; QuoteRequest resolution only ever checked the old actor's `/notes/` prefix regardless of which actor's inbox received the request — both would have broken the moment the new actor had a real post).
- [x] **Add `contentMap` to outgoing Notes, fixing a spurious "Translate" button on every post** (2026-08-09) — flagged as a related-but-separate issue right at the start of the FEP-044f work and deliberately left out of scope at the time; came up again live once real posts were being read on Mastodon. Without `contentMap`, Mastodon can't tell what language a post is in and offers to translate it regardless. Confirmed the exact shape against a real Mastodon post's own JSON (`content` and `contentMap` are siblings; `contentMap` keyed by plain `"en"`, same HTML duplicated under it) rather than guessing. Fixed at both construction sites (`ap-posting.ts`, `scripts/announce.js`); `refresh-note` gained an `actor` field (`"old"`/`"new"`, defaults to old) so it can backfill either actor's already-published posts — used to fix the two posts (the pre-Move heads-up and the new actor's debut post) that had already gone out before this fix existed.
- [x] **Bot's own ActivityPub actor still isn't showing a verified profile link** (root-caused and fixed 2026-08-10, live confirmation pending) — two earlier attempts (2026-08-01, 2026-08-03) guessed at the mechanism and both left the badge unverified. This time root-caused directly against Mastodon's own source (`VerifyLinkService` + `ActivityPub::TagManager#uri_for`, see the 2026-08-10 changelog entry): for a *remote* account, the rel=me link Mastodon looks for must match `account.uri` — populated from the actor's AP `id` (`.../actor`) — not its `url` field. The homepage's `<link rel="me">` tags pointed at the bare domain the whole time, so neither earlier attempt could ever have worked regardless of caching. Fixed in `layout.tsx` for both actors; Pat's own personal-account link was already correct as-is (his account is local to mastodon.nz, where `uri_for` resolves differently). Still not confirmed live as *showing* verified a full day later (2026-08-11) — same for the follower count and followers/following-list visibility, which stayed stale on the same instances. The `Person`-only-gating theory floated back on 2026-08-01 is now definitively ruled out (`VerifyAccountLinksWorker` processes every account type uniformly, confirmed against source) — see the 2026-08-11 changelog entry and `POST /api/admin/refresh-actor`, which sends a signed `Update{Actor}` to force the refresh on followers directly rather than waiting on an incidental trigger.
- [ ] **NZ-specific vocabulary** — build a word bank ("munted", "choice", "sweet as", "mean as", "stoked", "gutted", "staunch") to weave into quips in `quips.ts`
- [ ] **A pool of "good day" photos, cycled into share links and the daily announcement** — user-taken photos (uploaded via Vercel Blob, or whatever store the radomski blog already uses for images — check that project's actual setup rather than assuming), with one selected (randomly, or rotating to avoid immediate repeats — open question) to serve as: the OG/Twitter share-card image (currently a single static `canyoubeatwellington_og_image.jpg`, would need to become dynamic per day/request rather than a build-time asset) and/or an image attachment on the daily ActivityPub Note (`ap-posting.ts` currently posts text-only — AP's `attachment: [{ type: "Document", mediaType: "image/...", url: "..." }]` shape is what most software, Mastodon included, expects for an embedded post image). Worth deciding whether the OG image and the announcement's image should be the *same* pick for a given day (likely, for consistency between what someone sees sharing the link vs. seeing the toot) before building either half. Needs an upload/management path too — most likely a small admin-gated route mirroring the existing `ADMIN_SECRET` pattern, rather than editing KV/Blob by hand each time. Also want the date taken and location burned into the bottom of each photo (server-side image annotation at upload/serve time, not just filename/alt-text metadata) — gives each share/post its own bit of context ("Oriental Bay, 12 Jan 2026") rather than an anonymous stock-looking photo.
- [x] **User sharing** (2026-06-29) — `src/components/ShareButtons.tsx` on the About page. Bluesky uses its one compose-intent URL; Mastodon has no single domain, so the button asks once for the user's instance and remembers it in `localStorage` for next time, then opens that instance's `/share?text=` compose URL. Plus a copy-link fallback. No API keys, no new data fetch — share text is a static line, matching the About page's existing fully-static nature.
- [ ] **Resolve the All Blacks v Italy fixture** (`all-blacks-v-italy-2026-07-11`) after the 2026-07-11 test — `POST /api/admin/resolve-sporting` with the result, exercising the semi-automated sporting workflow live for the first time. Since 2026-07-02 the endpoint answers 404 (unknown id) / 409 (already resolved) instead of a silent `ok` — a typo'd occurrence id will now be obvious.
- [ ] **Keep moveable special dates current** — Matariki/CubaDupa/WOW/Beervana occurrences only run through 2026–2027; add next year's real dates as they're announced (`special_date_occurrences`, manual insert — `fixed_rule` holidays self-heal via the cron, these don't)
- [ ] **"Can You Beat Wellington Wrapped"** — a Spotify-Wrapped-style year-in-review, spec'd 2026-08-17, not yet built. **Automated, not hand-built each year** — every requested stat is already derivable at runtime from data the app continuously stores (`daily_weather_records` back to 2020, `special_date_occurrences`), same "compute live from history" pattern `weatherFunFacts.ts` already uses, just year-scoped and on its own page — building it generically once means no manual work each January, and it's more consistent with how the rest of the app works than a hand-rebuilt page would be. Scope for a given calendar year (Jan 1–Dec 31, not a rolling window):
  - Total "bad" days — count days where `resolveVerdict()` (weather + special-date override) is false, reusing existing verdict logic rather than a new definition.
  - Top season / all seasons ranked by good-day count — group by `getSeasonLabel()`. Note `rulesStorage.ts` actually has 7 season buckets internally (Late Autumn split from Autumn) even though the doc prose above says "six seasons" — use whatever `getSeasonLabel()` actually returns rather than assuming 6.
  - Total agree/disagree votes — sum the existing `agree_count`/`disagree_count` columns (already stored per day, no new collection needed).
  - Whether people are more likely to disagree on a good-verdict day or a bad-verdict day — join vote counts to that day's verdict, compare disagree-rate between the two groups; a real computable stat, not a guess.
  - A few fun facts — extend `weatherFunFacts.ts`, year-scoped: rainiest day, longest good/bad-day streak, snow-day count, how many days a special-date override flipped the verdict (e.g. "the Hurricanes saved you a day").
  - Rain days count/percentage — `rain > 0` count over days in the year, matching the existing all-time fun-fact convention.
  - Wind speed distribution — bar or pie chart, binned (e.g. 0–10/10–20/20–30/30–40/40+ km/h) from the stored `wind_speed` field. Hand-rolled SVG, not a new charting dependency, consistent with this app's existing zero-dependency component style — **load the `dataviz` skill before writing any chart code when this actually gets built.**
  - Route: `/wrapped` (or `/wrapped/[year]` from the start, so future years are free once it exists). Build and test it whenever, but **don't link it from the home page nav until the actual release** — same "real URL, deliberately unlinked until intended" pattern already used for the AP followers/following HTML pages, satisfies "prepare but don't release yet" with no feature flag needed.
  - Optional, lower-priority extension: a one-off ActivityPub Note announcing it, reusing `postToFollowers`/`deliverToFollowers` (same shape as a special-date post), triggered manually like the existing `scripts/announce.js` workflow rather than tied to the daily cron.
  - Target: ready in time to release on New Year's Eve or New Year's Day.

### P5 — Nice to have
- [ ] **Timezone-aware date handling** — dates are stored as `YYYY-MM-DD` strings; a few places still parse them with `new Date(dateString)` (UTC midnight) instead of `date-fns/parseISO`, risking a ±1 day shift in NZ timezone (UTC+12/+13). Audit remaining call sites, particularly around `CalendarHistory`.

### P5 — Widgets & embeds
- [ ] **Embeddable widget for other websites** — small `<iframe>`-able route (e.g. `/widget`) showing today's verdict with no nav/chrome, sized for embedding via a copy-paste `<iframe>` snippet (simplest approach, avoids CORS entirely). Comparatively low effort — same Next.js app, one new minimal-layout route.
- [ ] **Desktop widget — macOS** — requires a native Swift/SwiftUI app with a WidgetKit extension; not web technology. Needs a small public JSON endpoint (e.g. `/api/widget-data`) for the widget to poll.
- [ ] **Desktop widget — Windows** — needs a native app (WinUI, or a thin web-view wrapper) calling the same JSON endpoint.
- [ ] **Mobile widget — iOS** — home screen widget via WidgetKit (Swift); could share most of a native shell with the macOS widget.
- [ ] **Mobile widget — Android** — home screen widget via Glance/RemoteViews (Kotlin).
  - **Scope note**: the four native widgets are a different category of effort from the rest of this backlog — each needs a real native app shell (Swift for Apple platforms, Kotlin for Android), code signing, and app store distribution (or at minimum local sideloading), not just a web feature. Worth treating as a separate mini-project if pursued. The embeddable web widget is far simpler and could ship first as a stepping stone — same underlying data, no native shell needed.

---

## Changelog

- **2026-08-27** — Widened the home page's `revalidate` from 600s to 3600s (matching `/history`'s existing value), prompted by Neon compute-hour usage running at 83%+ of the free tier's monthly allowance. Investigated and ruled out the obvious suspects first: autosuspend was assumed disabled per an old note in this file ("no auto-pause, unlike Supabase"), but `suspend_timeout_seconds: 0` on the compute endpoint actually means "use Neon's default 5-minute timeout," confirmed both against Neon's docs and the endpoint's own lifetime `active_time_seconds` (~17% of elapsed wall-clock time, consistent with genuine suspend/resume cycling) — the old note likely just meant "no Supabase-style whole-project pause," not "no autosuspend at all." Table sizes were also checked directly: the entire database is under 500KB, so query cost isn't a factor either. Confirmed directly in the Neon console that the free tier fixes scale-to-zero at 5 minutes with no way to shorten it without upgrading — leaving revalidate frequency as the only real lever, since a 10-minute window meant most real visits woke the compute from an already-suspended state (10 min > the fixed 5-min timeout) rather than hitting a still-warm one, paying the idle tail on nearly every regeneration. Trade-off: a wider window for a cached page to show yesterday's date/verdict right after NZ midnight before self-correcting, same as `/history` already accepts.
- **2026-08-17** — Narrowed `middleware.ts`'s `config.matcher` to also exclude the AP-identity path prefixes (`/actor`, `/.well-known/webfinger`, `/notes`, `/quote-authorizations`), prompted by investigating sustained-high Fluid CPU usage. Confirmed via Vercel's own runtime logs (grouped by source, then by path) that `edge`/`middleware` was the dominant cost category, and `/actor/inbox` alone accounted for ~65% of middleware invocations in a 24h sample (mostly rejected signed spam, unrelated to this app) — of ~354 total middleware runs that day, only 60 ever produced a redirect. `getRedirectUrl()` already never redirects the AP paths regardless of domain (existing, tested behaviour), so excluding them from the matcher is a pure invocation-count reduction with no behaviour change — confirmed by directly testing the compiled matcher regex (not just the redirect-decision function) against real app routes, AP sub-paths, and a production build. `/` (the human-facing home page, which genuinely can't skip the host check) remains the only other real contributor.
- **2026-08-17** — Fixed a real production crash: the home page 500'd for about an hour overnight (~02:17–03:17 NZT, 5 visitors, ~250 occurrences) with `TypeError: Cannot read properties of null (reading 'toFixed')`. Root cause: `fetchLiveWeather()`'s forecast-array builder in `weather.ts` read `data.daily.temperature_2m_max[i + 1]` directly with no null guard, unlike every other field in the same function — Open-Meteo can return an explicit `null` for a given forecast day (not just a short array), which then hit `.toFixed()` unguarded inside `ForecastStrip`'s `.map()`. Also tightened the existing "today" validation, which only checked `=== undefined` and would have let the same `null` value slip through for day 0 too. Incomplete forecast days are now filtered out rather than assumed valid — a 5- or 6-day strip beats a broken page. Two regression tests added (explicit-null-for-today throws, explicit-null-for-a-forecast-day drops that day without crashing) using the existing mocked-fetch test scaffolding.
- **2026-08-16** — Added a negative cache (`cybw:ap:bad-actor:<url>`, 1hr TTL) to `fetchActorPublicKey()` in `http-signatures.ts` — prompted by investigating a rise in Fluid CPU usage, which turned out to correlate with a growing volume of rejected `/actor/inbox` spam (13,000+ rejections in the retained log window, with "Malformed Signature header" and "Signature verification failed" both recent, faster-growing patterns — the latter only 10 days old). Every rejected signed request that gets as far as a claimed actor URL currently pays a full outbound fetch (up to the 10s timeout) to resolve that actor's public key before failing; since spam frequently replays the same handful of dead/slow actor URLs, caching the failure short-term avoids re-paying that cost on every replay. TTL is short rather than permanent since a failure could be transient. No unit test added — matches this codebase's existing convention that KV-touching logic is verified live rather than unit tested (only pure functions get direct tests here).
- **2026-08-14** — Added an "almost good day" quip tier: `isAlmostGoodDay()`/`ALMOST_GOOD_DAY_QUIPS`/`pickAlmostGoodDayQuip()` in `quips.ts`, checked in `page.tsx`'s verdict-line precedence right after `isGreatDay` — fires when wind and rain both clear the normal good-day bar and temperature falls short of the seasonal minimum by less than a full degree. Reuses the caller's own `windMet`/`rainMet` rather than a separate hardcoded bar (unlike `isGreatDay`, which intentionally checks a *stricter* threshold of its own). Prompted by, and verified live against, an actual near-miss day (12.3°C vs Winter's 13°C minimum, wind/rain both fine).
- **2026-08-11** — Added `POST /api/admin/refresh-actor` (`ADMIN_SECRET` bearer auth) — sends a signed `Update{Actor}` to an actor's current followers, prompted by the rel=me badge and follower count still not updating a full day after the 2026-08-10 fix. Root-caused further via Mastodon's own source: `ResolveAccountService`'s `possibly_stale?` (1-day threshold) only fires on a genuine resolve — search-by-handle, a new follow, an incoming `Update{Actor}` — not on an ordinary page view of an already-cached profile, and `UpdateAccountService` (which applies fresh data and unconditionally re-runs `VerifyAccountLinksWorker`, confirmed no `Person`-only gating — ruling out the oldest open theory from 2026-08-01) is what a receiving instance runs against that Update. This gives a way to force the refresh on followers directly instead of waiting on an incidental trigger; it can't reach non-follower instances that only ever searched the account once — those need a person on that specific instance to search the full handle again. Extracted `buildActorDocument()` into `ap-identity.ts` as the single source of truth for an actor's JSON document (previously only inline in `actor/route.ts`) so the Update's embedded object and `GET /actor` can never drift apart.
- **2026-08-10** — Updated the daily announcement's opening line to "You can't beat Wellington on a good day! And today is a good day!" (was "...today ☀️"), and added `#Wellington #CYBW #YCBWOAGD` to the post body. `postToFollowers()` gained an optional `hashtags` parameter that builds a proper AP `tag` array of `Hashtag` objects — confirmed this is what Mastodon actually relies on to index/search a remote post under a tag, not just the visible "#word" text in content — scoped to the daily cron's call site only, other callers unaffected.
- **2026-08-10** — Relaxed the rain criterion from a literal 0mm to `maxRain: 0.1` (all seasons, `rulesStorage.ts`), prompted by a day that met every other criterion with only 0.1mm of still-forecast (not yet fallen) afternoon rain in the daytime total. Open-Meteo's hourly precipitation figures blend actual-so-far and forecast-for-the-rest-of-day into one live-recomputed sum, so this needed no new "has it actually happened yet" tracking — the same daytime sum already self-corrects on the next fetch if more rain falls later, dropping the day back below the threshold automatically. `isGreatDay` in `quips.ts` deliberately keeps its own stricter literal `rain <= 0` check, unaffected by this — a "great day" upgrade should still mean genuinely dry, not just within the good-day tolerance.
- **2026-08-10** — Added an HTML view to `actor/followers`/`actor/following`, content-negotiated on the same URL already serving the AP JSON collection (`src/lib/collection-html.ts`) — a plain browser (no `Accept: application/activity+json`/`ld+json`) now gets a simple linked list of actor URLs instead. Prompted by discovering that a remote Mastodon instance's own web UI can never show a *remote* account's full followers/following list, regardless of how correctly the collection is shaped — it only ever renders relationships it's locally observed via federation (confirmed against Mastodon's own GitHub issues, not guessed). Real Mastodon instances work around this with a "browse on the original profile" link to a human-facing page at the account's own origin; this adds that page for our own actors. Not linked from the site's own nav — reachable only by visiting directly.
- **2026-08-10** — Fixed the bot's own `rel="me"` actor-verification link, which had never worked despite two earlier attempts (see 2026-08-01/2026-08-03 entries below) — root-caused this time against Mastodon's actual source (`VerifyLinkService` + `ActivityPub::TagManager#uri_for`) instead of guessing again. For a *remote* account, the reciprocal link Mastodon looks for must match `account.uri` (populated from the actor's AP `id`, e.g. `.../actor`), not its `url` field — the homepage's `<link rel="me">` tags pointed at the bare domain instead, so verification could never have succeeded regardless of caching. Fixed in `layout.tsx`.
- **2026-08-10** — Added incoming-boost (`Announce`) tracking: a new per-actor `cybw:ap:boosts`/`cybw:ap:boosts:new` KV list, populated by a new `handleAnnounce()` in the inbox route using the same anti-spoof + "is this really one of our posts" validation already used for `QuoteRequest`. Internal record-keeping only, requested for the user's own reference — separate from, and with no effect on, Mastodon's own native per-post booster display.
- **2026-08-09** — Completed the ActivityPub actor migration from `canyoubeatwellington.radomski.co.nz` to `canyoubeatwellington.nz`, via a real FEP-compliant Move rather than just the domain redirect (which was already in place and stayed unchanged). Built the new actor's identity infrastructure (`src/lib/ap-identity.ts`, replacing 8 hardcoded domain declarations), gave it its own keypair, `alsoKnownAs`/`movedTo` cross-references, a fresh bio and share/banner image (a real waterfront photo replacing the old generated graphic), and an About-page copy-to-clipboard handle. Sent a heads-up post from the old actor, then the Move itself, then a debut post from the new one — all three confirmed delivered via GitHub Actions/Vercel logs, not just assumed. Verified the actual migration mechanics via direct inbox log evidence (`Follow`-to-new immediately followed by `Undo`-to-old for each migrating follower) rather than only before/after follower-count snapshots — one follower's migration timing showed it self-migrated based on the live `movedTo` field before the Move was even sent, ahead of Mastodon's own async processing of the explicit activity. Switched daily posting (`postToFollowers()`) to the new actor once migration was confirmed, and generalized `deliverToFollowers()`/the outbox route/`QuoteRequest` handling to work correctly for whichever actor needed it, rather than assuming one — the outbox for the new actor was hardcoded to always show empty, and quote resolution only ever checked the old actor's note prefix, both of which would have broken the moment the new actor had a real post. Along the way, fixed two unrelated-but-discovered issues live: WebFinger username matching was case-sensitive (a pre-existing bug on *both* actors, `@canyoubeat@...` 404'd even though `@CanYouBeat@...` worked, never noticed before since existing followers found the account via a link rather than typing the handle), and outgoing Notes had no `contentMap`, so Mastodon offered a "Translate" button on every post regardless of actual language — confirmed the fix's exact shape against a real Mastodon post's own JSON. Also switched the human-facing canonical domain from `www.canyoubeatwellington.nz` to the bare apex (Vercel's own domain redirect flipped `www → apex`, replacing the original `apex → www`), which simplified the new actor's identity (no more split between its WebFinger handle domain and its actual endpoint domain, needed only to route around the old redirect direction).
- **2026-08-07** — Completed FEP-044f quote-post support: fixed `automaticApproval` being an array instead of the spec's bare string, and implemented the `QuoteRequest`/`QuoteAuthorization` inbox handshake that was the actual reason quotes sat "pending" forever even with the interaction policy declared. New `GET /quote-authorizations/[id]` endpoint, added to the AP-pinned path list in `middleware.ts`. Verified live: a real quote from `@Pat@mastodon.nz` against a real post succeeded, confirmed via Vercel runtime logs (`Inbox: approved QuoteRequest`, followed by mastodon.nz fetching the new authorization and getting 200), not just the UI looking right. See the P4 backlog entries above for the full technical detail. Also this session: added the `canyoubeatwellington.nz` → `www.canyoubeatwellington.nz` redirect for all human-facing traffic while keeping the ActivityPub actor pinned to the old domain (`middleware.ts`), fixed every other stale old-domain reference found in the process (share buttons, sitemap, robots.txt, README, the daily post's body link, admin-route doc comments), and added Open Graph/Twitter card metadata from scratch — there wasn't any before, which is why a raw `&#x27;` entity was showing up in share previews instead of an apostrophe (fixed by rewording the description rather than relying on every unfurler to decode correctly).
- **2026-08-03** — Two infrastructure additions, deployed same day once the Neon migration ran: (1) `special_date_defs.scenario_quips` — special dates can now have weather-scenario-dependent quips (e.g. a different line for a rainy Christmas than a cold one) instead of only a single fixed `quip_override`; `getSpecialDayScenario()`/`getSpecialDayQuip()` in `special-dates-logic.ts` resolve it, checked in `page.tsx`'s verdict-line precedence between the global severity quips and the global great-day/standard quips. No content written yet — the actual Christmas/NYE/NYD quips stay in the batched quip backlog until there's a reason to ship. (2) `daily_weather_records.feels_like` — daytime-minimum apparent temperature (wind chill/humidity/radiation-adjusted, from Open-Meteo's `apparent_temperature`) is now fetched and stored alongside temp/wind/rain, prompted by today's actual 9°C reading feeling like -3 to -7°C on a very windy day. Not displayed anywhere yet and no quips reference it — captured now so it's available whenever quips do.
- **2026-08-03** — `daily_weather_records.snowfall` (daily total, cm, from Open-Meteo's `snowfall_sum`) — prompted by an actual light snow shower in Wellington that day (confirmed in the live API data: WMO code 85 at three separate hours, ~0.7cm total), which is rare enough locally to make the news, hence wanting to call it out. Data capture only in this commit — the quip tier followed later the same day, once there was text for it (see below).
- **2026-08-03** — Shipped the first batched quip release: a new ALL_BAD quip; two new severity tiers, `SNOW` (any snowfall, checked first — ahead of even `WIND_60` — since Wellington snow is rare enough to make the news) and `FEELS_LIKE_COLD` (`feels_like < 0`, checked after the wind tiers so a wind-driven cold snap doesn't double up with the wind quip); and `scenario_quips` content for three special dates — Christmas Day (good/rain/cold/all_bad), New Year's Day (good), and a brand new `new-years-eve` def (good) that didn't exist as a `special_date_defs` row before today. `getSeverityScenario()`/`pickSeverityQuip()` moved from positional `(windSpeed, rain)` args to a single `SeverityWeather` object now that there are four inputs (`windSpeed`/`rain`/`snowfall`/`feelsLike`) — cleaner than a five-argument function. Released together rather than piecemeal specifically so the SNOW tier would be live in case it snowed again; `new-years-eve`'s first occurrence self-generates on the next daily cron run via `ensureUpcomingOccurrences()`, no manual seeding needed.
- **2026-08-03** — Added FEP-044f `interactionPolicy` (public quote consent) to every outgoing Note, fixing Mastodon's outright "you are not allowed to quote this" block on the daily good-day posts — the exact JSON-LD `gts:` context terms were verified against a live Mastodon post's own ActivityPub JSON rather than trusted from third-party write-ups, which disagreed with each other. The deeper `QuoteRequest`/`QuoteAuthorization` inbox handshake FEP-044f also describes is not yet implemented (P4 backlog) — no verified-exact JSON shape found for it yet, unlike the static field. Separately investigated why the bot's own actor profile link still doesn't show verified on Mastodon after the 2026-08-01 fix attempt (P4 backlog) — confirmed via user report that the self-referential `rel="me"` theory hasn't produced a visible verified badge; two follow-up theories logged, neither confirmed.
- **2026-07-24** — Moved the daily cron from 12:00 UTC (≈midnight NZT — originally chosen as "midnight NZST" pre-migration, see `CLAUDE_ARCHIVE.md`) to 21:30 UTC (≈10am NZT), after feedback that the ActivityPub good-day announcement was firing in the middle of the night and reads oddly to followers. Vercel Cron has no timezone option (UTC only), so a fixed schedule can't track NZ's daylight-saving transition; 21:30 UTC splits the difference evenly (10:30am NZDT / 9:30am NZST) rather than favouring one season. Config-only change (`vercel.json`), no code touched. Worth knowing: the "good day" verdict used for the fan-out decision is whatever Open-Meteo returns for the still-partially-elapsed day at cron time — the daytime window is 6am–6pm, so at 10am roughly a third of it is observed and the rest is same-day forecast. The stored record self-corrects with fully observed data on the *next* day's cron run (`fetchAndStoreBatch` re-upserts all 92 past days every run), but a post that already went out doesn't get retracted if the afternoon doesn't pan out as forecast — pre-existing behaviour, not introduced by this change, just more exposed by moving off a near-midnight run where forecast/actual mattered less to anyone watching the clock.
- **2026-07-24** — Fixed the browser tab favicon, which had shown Vercel/Next's default triangle logo since the migration: `src/app/favicon.ico` was a leftover `create-next-app` scaffold file, never replaced with the real design (a black-circle "W" mark, `public/favicon.svg`, added pre-migration but never wired into Next's file-based icon convention). Rebuilt `favicon.ico` from the SVG at 16/32/48px (hand-assembled to avoid a third-party ico tool bloating it with a blurry auto-upscaled 256px frame), and added `src/app/icon.svg` so modern browsers get a crisp SVG favicon. Also deleted `public/favicon.ico` — an unrelated, unreferenced terminal-icon file dating back to the original pre-Lovable project scaffold, dead weight since day one.
- **2026-07-02** — Security/correctness hardening pass across the migration + special dates work: ActivityPub inbox now verifies the signed `Digest` against the body, requires `(request-target)/host/date/digest` in the signature, rejects stale `Date` headers, and matches the body's `actor` against the signing key's actor (previously any fediverse account could spoof Follow/Undo for someone else); vote Server Action validates type/token and only accepts today's date (historical counts were client-rewritable); cron auth fails closed and both bearer checks are constant-time (`src/lib/auth.ts`); History calendar now applies `verdict_override` via shared `isGoodWeatherDay()`/`resolveVerdict()` (it was showing the Hurricanes-final forced good day as a red ✗); `resolve-sporting` returns 404/409 on no-ops instead of `ok`; AP fan-out parallelised with per-request timeouts, KV-cached inbox URLs, and a 1-year TTL on stored notes; dark special backgrounds (matariki/rugby) now swap to readable light text; home `revalidate` 3600→600 to shrink the stale-page window at NZ midnight; pure special-dates logic split into client-safe `special-dates-logic.ts`; Neon schema snapshot checked in at `db/schema.sql`. Deployed to production the same day (`staging` → fast-forward `main`, commit `e8ca49d`) and verified live: CI green, homepage/webfinger responding, unique index on `special_date_occurrences (def_id, start_date)` created in prod by hand, and the hardened inbox confirmed end-to-end via an unfollow → re-follow round-trip from `mastodon.nz/@Pat` (all 3 followers intact, Accept delivered, KV inbox cache populated).
- **2026-04 to 2026-06** — Hardened and polished the original Vite + Supabase app: RLS policies, security headers, seasonal weather rules (the "Shitsville" calendar), tamper-resistant voting, historical backfill, scenario-based quips, 7-day forecast.
- **2026-06-08 to 2026-06-28** — Rebuilt the entire stack as Next.js (App Router) + Neon, in parallel on a `nextjs` branch, tested independently on a separate Vercel project, then cut over via `nextjs → staging → main`. Zero ActivityPub follower disruption (same actor URL/keys throughout). Full phase-by-phase history, schema, env var changes, risk register, and decision log archived in `CLAUDE_ARCHIVE.md`.
- **2026-06-28** — Shipped the special dates system (see above) and seeded the first real catalogue: 17 defs / 30 occurrences across NZ public holidays, Matariki/CubaDupa/WOW/Beervana, and two rugby fixtures (the Hurricanes' actual Super Rugby Pacific Grand Final win, and the upcoming All Blacks v Italy test).

---

## Architecture Notes

### Data flow
1. Home page (`src/app/page.tsx`, Server Component) → calls `getTodaysRecord()` (Neon), `fetchLiveWeather()` (Open-Meteo), and `getActiveSpecialDates()` (Neon) in parallel
2. If today's row doesn't exist yet, it's seeded immediately from the live fetch so voting has something to attach to before the daily cron runs; the cron later overwrites it with the final full-day reading (idempotent upsert)
3. Verdict computed server-side via `getThresholds(date)`/`countCriteriaMet()` from `rulesStorage.ts`, then passed through `resolveVerdict()` — a special date's `verdict_override` wins if set, otherwise the weather verdict stands. Same logic on every page, no home/history divergence
4. History/About pages fetch the full record set from Neon via `getHistoricalRecords()`/`getAllHistoricalRecords()`, plus `getSpecialDatesForRange()` for the calendar's sparkle badges
5. Votes: `VotingButtons` (client component) calls the `castVoteAction` Server Action → `castVote()` in `src/lib/votes.ts` → `increment_vote()` Postgres function in Neon; a `vote_tokens (token, date)` unique constraint blocks duplicate votes per browser identity per day
6. Daily cron (`/api/cron/daily-weather`, Vercel Cron at 21:30 UTC ≈ 10am NZT) re-fetches 92 days of Open-Meteo data, upserts to Neon, regenerates next year's fixed-rule special dates if missing, and fans out an ActivityPub `Create{Note}` to followers via Vercel KV + signed HTTP delivery if today is a good day (weather or override)

### Key files
| File | Purpose |
|------|---------|
| `src/lib/weather.ts` | Neon queries — `getTodaysRecord`, `getHistoricalRecords`, `getAllHistoricalRecords`, `upsertWeatherRecord`, `fetchLiveWeather`, `fetchAndStoreBatch`, `getTodaysNZTDate`; also computes the daytime `feels_like` (coldest apparent temp, 6am-6pm) and captures daily `snowfall`, alongside temp/wind/rain |
| `src/lib/special-dates.ts` | Special-dates DB layer — `getActiveSpecialDates`, `getSpecialDatesForRange`, `ensureUpcomingOccurrences`, `resolveSportingOccurrence`; re-exports all of `special-dates-logic.ts` |
| `src/lib/special-dates-logic.ts` | Pure special-dates logic + types (`pickPrimarySpecialDate`, `resolveVerdict`, `computeFixedRuleDate`, `getSpecialDayScenario`, `getSpecialDayQuip`) — no DB import, safe for client components like `CalendarHistory` |
| `src/lib/auth.ts` | `isBearerAuthorized()` — constant-time, fail-closed bearer check shared by the cron and admin routes |
| `db/schema.sql` | Reference snapshot of the Neon schema (tables, indexes, `increment_vote()`) — documentation, not a migration runner |
| `src/lib/votes.ts` | `castVote()` — wraps `increment_vote()`, translates the `23505` unique-violation into `{ alreadyVoted: true }` |
| `src/lib/db.ts` | Exports the `sql` Neon client (`@neondatabase/serverless`), reads `DATABASE_URL` |
| `src/lib/ap-identity.ts` | Shared ActivityPub identity module — `OLD_ACTOR`/`NEW_ACTOR` (id, keys, KV key names for each), `actorForHost()` (Host header → actor, for `/actor`/`inbox`/`outbox`/`followers`), `actorForWebfingerDomain()` (the WebFinger `resource` param's domain part → actor), `alsoKnownAs`/`movedTo` cross-references, `buildActorDocument()` (the single source of truth for an actor's JSON document, shared by `GET /actor` and `refresh-actor`) |
| `src/lib/ap-move.ts` | `buildMoveActivity()` — constructs the FEP-compliant `Move` (old actor → new actor); pure/testable, doesn't send anything itself |
| `src/lib/ap-posting.ts` | `postToFollowers()` — Note+Create assembly and fanout delivery for the daily cron, posts as `NEW_ACTOR`. `deliverToFollowers()` (shared fan-out helper, also used by `refresh-note`/`send-move`) takes the actor explicitly since different callers need different identities |
| `src/lib/http-signatures.ts` | HTTP signature sign/verify for ActivityPub delivery and inbox auth. Negative-caches (`cybw:ap:bad-actor:<url>`, 1hr TTL) actor URLs that recently failed to resolve, so repeat inbox spam replaying the same dead/slow actor doesn't re-pay the outbound fetch each time |
| `src/utils/rulesStorage.ts` | Seasonal weather criteria — `getThresholds(date)`, `getSeasonLabel(date)`, `countCriteriaMet(weather, date)`, `isGoodWeatherDay(weather, date)` (the single good-weather-day definition used by home/cron/calendar) |
| `src/utils/quips.ts` | Scenario-based verdict quips + forecast summary quips |
| `src/utils/specialBackgrounds.ts` | Code-side `background_key` → Tailwind gradient lookup (never raw CSS from the DB) |
| `src/utils/weatherFunFacts.ts` | Fun fact generation from historical data |
| `src/types/db.ts` | `DailyWeatherRecord` type matching the Neon schema |
| `src/app/page.tsx` | Home page — Server Component, the critical path |
| `src/app/history/page.tsx`, `src/app/about/page.tsx` | Server Components for historical charts/calendar and static rules content |
| `src/components/SpecialDateEffect.tsx` | Confetti (dynamically imported `canvas-confetti`) / balloon (CSS keyframes) celebration overlay |
| `src/components/FediverseHandle.tsx` | About page's fediverse handle display with a copy-to-clipboard button (same pattern as `ShareButtons`' copy-link button) |
| `src/app/api/cron/daily-weather/route.ts` | Daily cron handler — fetch, upsert, regenerate special dates, evaluate verdict, fan out on good days |
| `src/app/api/admin/resolve-sporting/route.ts` | Semi-automated sporting-result resolution, `ADMIN_SECRET` bearer auth |
| `src/app/api/admin/refresh-note/route.ts` | Re-stamps a previously-published note with the current `interactionPolicy`/`contentMap`/etc. shape and sends a signed `Update` to followers — for backfilling posts published before some later field existed. Takes an `actor` field (`"old"`/`"new"`, defaults to old) since posts now exist under either. `ADMIN_SECRET` bearer auth |
| `src/app/api/admin/refresh-actor/route.ts` | Sends a signed `Update{Actor}` to an actor's current followers, forcing instances that already have a cached copy to redo their resolve (and, per Mastodon's `UpdateAccountService`, re-verify `rel=me` links) rather than waiting on an incidental trigger. Only reaches followers, not non-follower instances that merely viewed the profile once. Takes an `actor` field (`"old"`/`"new"`, required). `ADMIN_SECRET` bearer auth |
| `src/app/api/admin/send-move/route.ts` | Builds and delivers the signed `Move` (old actor → new actor) to the old actor's current followers via `deliverToFollowers()`. Not called anywhere automatically — a genuinely one-way, fediverse-visible action, `ADMIN_SECRET` bearer auth |
| `src/app/actor/route.ts`, `src/app/actor/inbox/route.ts`, `src/app/actor/outbox/route.ts`, `src/app/actor/followers/route.ts`, `src/app/.well-known/webfinger/route.ts`, `src/app/notes/[id]/route.ts`, `src/app/quote-authorizations/[id]/route.ts` | ActivityPub endpoints, placed at their public URLs directly (no rewrites needed). Host-aware via `ap-identity.ts` — the same routes serve either actor depending on which domain answered the request. `actor/inbox` also handles FEP-044f `QuoteRequest` activities (checking against whichever actor received the request), minting the `QuoteAuthorization` objects `quote-authorizations/[id]` serves. WebFinger matches the username case-insensitively |
| `src/middleware.ts` | 308-redirects `canyoubeatwellington.radomski.co.nz` straight to the bare apex `canyoubeatwellington.nz` for human traffic; excludes the AP-identity paths above both in the redirect decision (`getRedirectUrl()`, the pure, unit-tested logic) and in `config.matcher` itself, so the function isn't invoked at all for paths it can never redirect |
| `src/instrumentation.ts` | Forces IPv4-first DNS resolution at server startup (works around an intermittent undici/Open-Meteo connection issue) |
| `vercel.json` | Security headers, cache headers, Vercel Cron schedule |
| `.github/workflows/ci.yml` | Lint + type check + test + build + audit gate, runs on `main`/`staging` |
| `.github/workflows/announce.yml` | Manual one-off ActivityPub announcements via `workflow_dispatch` — `actor` input (`"old"`/`"new"`, defaults to new) selects which actor posts |
| `scripts/announce.js` | Delivers a one-off announcement to all of the selected actor's followers (called by `announce.yml`); auto-linkifies bare mentions of either of our own domains (guarded against linkifying just the domain half of an `@handle@domain` mention) |
| `scripts/gen-ap-keys.js` | One-off RSA key pair generator for the ActivityPub actor |
